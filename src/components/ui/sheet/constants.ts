export const SHEET_VIEW_STATE = {
    OPEN: "open",
    CLOSED: "closed",
} as const;

export const SHEET_CONTROL_MODE = {
    OPEN: "open",
    VIEW: "view",
    BOTH: "both",
} as const;

export const SHEET_SIDE = {
    TOP: "top",
    RIGHT: "right",
    BOTTOM: "bottom",
    LEFT: "left",
} as const;
